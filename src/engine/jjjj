def MaxUsers(Max):
        """ generated source for method MaxUsers """
        # HACK: Change when I add more images to support more players.
        if Max<2:
            totalplayers = 2
        else:
            totalplayers = Max
        if Max > 4:
            totalplayers = 4

print MaxUsers(3)
